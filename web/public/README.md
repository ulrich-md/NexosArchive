# public/

Vite sirve lo que hay aquí tal cual, sin procesarlo, en la raíz del sitio.

Sube aquí el logo real de Nexos como `logo.png` (o `.svg`): GitHub → "Add file" →
"Upload files" → arrastra la imagen a esta carpeta → commit a
`claude/nueva-sesion-ituzea`.
